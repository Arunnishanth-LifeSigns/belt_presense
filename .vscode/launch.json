{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Belt Presense (Local)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go",
            "envFile": "${workspaceFolder}/.env",
            "buildFlags": "-tags=CGO_ENABLED_1"
        }
    ]
}